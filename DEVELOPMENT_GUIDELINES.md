# 个人开发准则
生成时间: 2026-05-19 16:21:52
基于最近 4 次Git提交的分析

## 提交消息规范

### 1. 提交消息应清晰具体

**问题**：发现模糊不清的提交消息，如单纯的'Update main.css'或'Fix bug'
**影响**：难以理解变更目的，不利于代码审查和历史追溯

**准则**：
- 使用约定式提交格式: `<type>(<scope>): <description>`
- 类型选项: feat(新功能), fix(错误修复), refactor(重构), docs(文档), test(测试), chore(杂务), perf(性能), ci(持续集成)
- 描述应具体说明做了什么改变，避免使用'Update','Modify','Fix'等泛泛而谈的词
- 良好示例: `fix(layout): 修正导致内容区域与导航栏间距异常的CSS计算错误`
- 需要避免: `Update main.css`, `Fix bug`, `changes`

### 3. 从错误中学习，防止类似问题再次发生

**问题**：提交历史显示反复出现类似的错误需要修复
**影响**：重复犯同样的错误浪费时间，影响代码质量

**准则**：
- 在修复错误后，思考：这个错误本可以如何被预防？
- 考虑添加断言、类型检查、单元测试或代码审查检查点
- 记录常见错误类型及其预防措施到个人知识库
- 在代码中添加注释说明为什么这样写可以避免已知问题

### 5. 通用编码最佳实践

**这些建议基于通用软件工程最佳实践而非特定提交分析**

**代码质量**：
- 每次提交前运行代码检查和测试
- 函数应专注于单一职责，建议不超过50行
- 文件应保持适度大小，建议不超过500行
- 有意义的变量和函数命名
- 适当注释解释为什么而非什么

**测试意识**：
- 优先编写测试（TDD理念）
- 为修复的错误添加回归测试
- 测试应覆盖正常路径和边界情况

**审查与反馈**：
- 代码提交前进行自行审查
- 善用工具进行静态代码分析
- 愿意接受和他人的代码审查反馈

---

*此准则由Git提交分析脚本自动生成*
*建议定期重新运行此脚本以更新准则*
*在开始新的编码工作前，请先阅读本准则*